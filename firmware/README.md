# RetroLink Firmware Artifacts

Generated UF2 firmware files are stored here.

Firmware source code lives in `../software/`.

The firmware version `1.00` build is expected to produce:

```text
retrolink-1.00.uf2
```